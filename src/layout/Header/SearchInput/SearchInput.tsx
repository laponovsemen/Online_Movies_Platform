import React from 'react';
import {S} from "./Styled_SearchInput"

const SearchInput = () => {
	/*todo неизвестно как на плейсхолдер нацепить значок увеличительного стекла через псевдоелементы плейсхолдера инпута не получается */
	return (
		<S.SearchInput>

		</S.SearchInput>
	);
};

export default SearchInput;
