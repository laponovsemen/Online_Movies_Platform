import styled from "styled-components";

const NewMoviesList = styled.div`
	text-transform: uppercase;
  height: 283px;
`

const NewMoviesListTitle = styled.div`
	text-transform: capitalize;
`
export const S = {
	NewMoviesList,
	NewMoviesListTitle
}
